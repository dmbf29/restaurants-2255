<div class="container">
  <h1>Add a Review</h1>
  <%# undefined method `reviews_path' %>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit 'Submit', class: 'btn btn-primary' %>
  <% end %>
</div>
